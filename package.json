{
  "name": "ai-hustle-hub",
  "version": "1.0.0",
  "description": "AI Money-Making Guide Website",
  "main": "index.js",
  "scripts": {
    "build": "npm install && cd netlify/functions && npm install",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@netlify/functions": "^2.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 